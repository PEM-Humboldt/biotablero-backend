{
  "name": "backend",
  "version": "1.3.0",
  "description": "Main backend service for Biotablero (sancochito)",
  "main": "src/app.js",
  "scripts": {
    "start": "nodemon --watch src --watch config",
    "start-prod": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "yarn g:lint src",
    "docs": "yarn g:docs -i api_doc/ -i src/ -o docs/"
  },
  "license": "MIT",
  "dependencies": {
    "bookshelf": "^1.2.0",
    "bottlejs": "1.7.1",
    "config": "2.0.2",
    "knex": "^0.21.16",
    "pg": "^8.7.3",
    "restify": "^8.6.0",
    "restify-cors-middleware": "1.1.1",
    "restify-router": "0.6.2",
    "winston": "3.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "apidoc": {
    "name": "Main services for Biotablero",
    "version": "0.1.0"
  }
}
